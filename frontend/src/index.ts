/**
 * ===============================================
 * MAIN EXPORTS
 * ===============================================
 * Central export point for the entire src module.
 * 
 * Usage:
 * import { useAuthStore, authService, ProtectedRoute } from "@/src";
 * // OR
 * import { useAuthStore } from "@/src/auth";
 */

// Re-export everything from auth module
export * from "./auth";
